// Template for WiFi credentials. Copy to include/secrets.h (gitignored) and
// fill in. This repo is public — secrets.h must never be committed.
#pragma once

#define WIFI_SSID     "your-ssid"
#define WIFI_PASSWORD "your-password"
